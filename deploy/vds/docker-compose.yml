services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ../../backend/sql/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - taksklad-internal

  backend-api:
    build:
      context: ../../backend
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL}
      TAKSKLAD_ENV: ${TAKSKLAD_ENV}
      TAKSKLAD_SERVICE_NAME: ${TAKSKLAD_SERVICE_NAME}
      TAKSKLAD_API_TOKEN: ${TAKSKLAD_API_TOKEN}
    depends_on:
      postgres:
        condition: service_healthy
    labels:
      - traefik.enable=true
      - traefik.http.routers.taksklad-backend.rule=Host(`${TAKSKLAD_BACKEND_HOST}`)
      - traefik.http.routers.taksklad-backend.entrypoints=websecure
      - traefik.http.routers.taksklad-backend.tls.certresolver=letsencrypt
      - traefik.http.services.taksklad-backend.loadbalancer.server.port=8000
      - traefik.docker.network=${TRAEFIK_NETWORK:-traefik}
    networks:
      - taksklad-internal
      - traefik

  skladbot-worker:
    build:
      context: ../../backend
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["python", "-m", "app.skladbot_worker"]
    env_file:
      - .env
    environment:
      DATABASE_URL: ${DATABASE_URL}
      SKLADBOT_API_TOKEN: ${SKLADBOT_API_TOKEN:-}
      SKLADBOT_API_BASE_URL: ${SKLADBOT_API_BASE_URL:-https://api.skladbot.ru/v1}
      SKLADBOT_API_TIMEOUT_SECONDS: ${SKLADBOT_API_TIMEOUT_SECONDS:-8}
      SKLADBOT_CUSTOMER_ID: ${SKLADBOT_CUSTOMER_ID:-6211}
      SKLADBOT_SHIPMENT_TYPE_ID: ${SKLADBOT_SHIPMENT_TYPE_ID:-3389}
      SKLADBOT_SYNC_LOOKBACK_DAYS: ${SKLADBOT_SYNC_LOOKBACK_DAYS:-1}
      SKLADBOT_REQUESTS_LIMIT: ${SKLADBOT_REQUESTS_LIMIT:-500}
      SKLADBOT_WORKER_INTERVAL_SECONDS: ${SKLADBOT_WORKER_INTERVAL_SECONDS:-600}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - taksklad-internal

  telegram-worker:
    build:
      context: ../../backend
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["python", "-m", "app.telegram_worker"]
    env_file:
      - .env
    environment:
      TAKSKLAD_API_TOKEN: ${TAKSKLAD_API_TOKEN}
      TAKSKLAD_BACKEND_INTERNAL_URL: http://backend-api:8000
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_ALLOWED_CHAT_IDS: ${TELEGRAM_ALLOWED_CHAT_IDS:-}
    depends_on:
      backend-api:
        condition: service_started
    networks:
      - taksklad-internal

  adminer:
    image: adminer:4
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    labels:
      - traefik.enable=true
      - traefik.http.routers.taksklad-adminer.rule=Host(`${TAKSKLAD_ADMINER_HOST}`)
      - traefik.http.routers.taksklad-adminer.entrypoints=websecure
      - traefik.http.routers.taksklad-adminer.tls.certresolver=letsencrypt
      - traefik.http.services.taksklad-adminer.loadbalancer.server.port=8080
      - traefik.docker.network=${TRAEFIK_NETWORK:-traefik}
    networks:
      - taksklad-internal
      - traefik

volumes:
  postgres_data:

networks:
  taksklad-internal:
  traefik:
    external: true
    name: ${TRAEFIK_NETWORK:-traefik}
