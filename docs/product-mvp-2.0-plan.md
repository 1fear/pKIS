# Product MVP 2.0 Plan

Дата фиксации: 2026-05-30.

Этот документ - обязательный план до релиза TakSklad 2.0. До выхода 2.0 новые задачи не добавляются в работу, если они не попадают в этот план. Исключения: блокировка склада, потеря данных, утечка секретов, критическая ошибка безопасности.

## 1. Цель Релиза 2.0

TakSklad 2.0 должен стать рабочей VDS-backed версией склада:

- desktop остается быстрым рабочим интерфейсом для сканирования, печати и локального fallback;
- backend на VDS становится центральной точкой для заказов, сканов, отчётов и истории;
- два рабочих компьютера могут работать параллельно без конфликтов через общий JSON/Google Sheets;
- SkladBot и Telegram работают на сервере, а не зависят от открытого desktop-приложения;
- данные можно восстановить из Postgres backup;
- релиз можно откатить понятной процедурой.

## 2. Что Входит В Product MVP

### Обязательно

- `api.taksklad.uz` указывает на VDS и открывает backend через HTTPS.
- Postgres backup работает по расписанию.
- Restore-drill выполнен на отдельной временной БД.
- Backend API имеет реальные endpoints для активных заказов, импортов, сканов, завершения заказа и дневного отчёта.
- Desktop умеет работать с backend за feature flag.
- Скан КИЗ сохраняется локально и отправляется в backend; при ошибке сети событие не теряется.
- Desktop может получить список заказов из backend.
- SkladBot worker на VDS подтягивает заявки и записывает результат матчинга в Postgres.
- Telegram worker на VDS работает как единственный слушатель Telegram.
- Импорт Excel идёт через backend.
- Менеджер задаёт дату отгрузки при Telegram-импорте, если источник не содержит надёжную дату.
- SkladBot matching сравнивает количество только в блоках, товар - по нормализованному цвету и формату, адрес - только как мягкий признак.
- Логистический отчёт формируется по выбранной дате отгрузки и содержит координаты доставки.
- Дневной отчёт формируется из backend/Postgres.
- Проведена ручная приёмка на Windows и на копии реальных заказов.
- `version.json` обновляется только после приёмки.

### Не Входит В 2.0

- Полноценная web-панель.
- Сложная ролевая модель пользователей.
- Аналитика склада и BI.
- Управление WMS/SkladBot write API.
- Большой редизайн desktop UI.
- Глубокий рефакторинг desktop без прямой связи с релизом 2.0.
- Новые функции, не влияющие на переход на VDS.

## 3. Этапы До Релиза

### Этап 0. Scope Lock

Статус: текущий документ.

Задачи:

- зафиксировать границы 2.0;
- держать все новые идеи в backlog, а не брать в работу сразу;
- перед каждым новым изменением проверять, к какому этапу оно относится.

Done:

- документ создан;
- дальнейшие задачи сверяются с этим планом.

### Этап 1. Production Foundation

Цель: серверная база должна быть не только поднята, но и эксплуатационно безопасна.

Задачи:

- настроить DNS `api.taksklad.uz`;
- проверить HTTPS на домене;
- провести restore-drill из свежего backup в отдельную временную БД/контейнер;
- добавить deploy/rollback runbook;
- решить минимальную миграционную процедуру для схемы Postgres до Alembic или с Alembic;
- проверить, что `.env`, токены, service credentials и backup-файлы не попадают в Git.

Done:

- `https://api.taksklad.uz/health` возвращает `200`;
- backup можно восстановить и прочитать таблицы;
- есть документированные команды deploy, rollback, backup, restore;
- локальные и VDS smoke проходят.

### Этап 2. Desktop Backend Bridge

Цель: desktop подключается к backend без риска остановить склад.

Задачи:

- добавить desktop config feature flag для backend;
- добавить backend API client в desktop;
- добавить safe dual-write сканов: локальный backup/очередь остаются первыми, backend получает копию;
- при ошибке backend не блокировать сканирование;
- добавить очередь повторной отправки backend-событий;
- добавить статус в UI: backend online/offline/sync pending;
- добавить чтение активных заказов из backend за отдельным флагом;
- сохранить fallback на текущий Google/local режим.

Done:

- при включённом флаге сканы уходят в backend;
- при выключенном флаге desktop работает как 1.1.7-линия;
- потеря сети не теряет КИЗ;
- два ПК могут сканировать разные позиции одного дня без конфликта в backend;
- тесты покрывают успех, timeout, duplicate, offline queue.

### Этап 3. Server Workers

Цель: Telegram и SkladBot перестают быть desktop-задачами.

Задачи SkladBot:

- добавить `skladbot-worker`;
- читать заявки по утверждённому окну;
- хранить raw response и нормализованный результат в Postgres;
- матчить заявки с заказами;
- записывать `skladbot_request_number`, status и причину `not_found/multiple`;
- не блокировать импорт и сканирование.

Задачи Telegram:

- добавить `telegram-worker`;
- хранить Telegram state/offset/lock в Postgres;
- принимать Excel и отправлять его в backend import;
- отправлять дневной отчёт из backend;
- убрать конфликт двух desktop по `getUpdates`;
- ограничить доступ по разрешённым chat_id.

Done:

- SkladBot работает без открытого desktop;
- Telegram работает без открытого desktop;
- HTTP 409 Telegram больше не возникает из-за двух запущенных приложений;
- импорт из Telegram появляется в backend;
- отчёт из Telegram совпадает с backend report.

### Этап 4. Real Workflow Acceptance

Цель: проверить продукт на складском сценарии, а не только endpoint smoke.

Сценарии:

- запуск desktop на Windows;
- импорт реального Excel;
- подтягивание номеров SkladBot;
- обновление списка во время сканирования;
- параллельная работа двух ПК;
- дубль КИЗ на одном и на двух ПК;
- отключение интернета во время сканирования;
- восстановление pending-событий после возврата сети;
- завершение заказа;
- печать;
- завершение дня;
- Telegram отчёт;
- backup после смены;
- restore-drill на свежих данных.

Done:

- все сценарии пройдены на тестовой копии реальных данных;
- список найденных дефектов закрыт или явно принят как known issue;
- нет блокирующих ошибок для склада.

### Этап 5. Release 2.0

Цель: выпустить 2.0 контролируемо, без внезапного auto-update.

Задачи:

- поднять версию приложения до `2.0.0`;
- собрать Windows archive;
- проверить archive на чистой Windows-машине;
- подготовить release notes;
- обновить `version.json` только после ручной приёмки;
- развернуть сначала на одном рабочем ПК;
- после смены без критичных ошибок включить второй ПК;
- оставить rollback-путь на 1.1.7/последнюю стабильную desktop-линию.

Done:

- 2.0 работает на рабочих ПК;
- backend получает реальные сканы;
- Telegram и SkladBot работают на VDS;
- есть свежий backup;
- есть понятный rollback.

## 4. Правила До Релиза 2.0

- Не собирать Windows release archive до прохождения этапов 1-4.
- Не менять `version.json` до этапа 5.
- Не отправлять push-уведомления об обновлении до ручной приёмки.
- Не добавлять новые функции вне этого плана.
- Не ломать desktop fallback.
- Не переносить секреты в Git.
- Любой найденный side-task записывать в backlog, но не брать в работу без привязки к этапу.

## 5. Текущий Статус

Готово:

- VDS поднят.
- Backend API MVP закрыт.
- Postgres работает.
- Traefik/HTTPS работают на временном домене.
- Backup timer включен.
- VDS staging smoke пройден.
- Restore-drill выполнен на отдельной временной БД.
- Deploy/rollback runbook добавлен.
- Desktop backend bridge добавлен за feature flags.
- Backend offline queue для desktop scan/complete событий добавлена.
- Desktop может читать активные заказы из backend при отдельном флаге.
- SkladBot worker добавлен как VDS-сервис.
- Telegram worker добавлен как VDS-сервис.
- VDS staging пересобран с `backend-api`, `skladbot-worker`, `telegram-worker`.
- Telegram worker теперь принимает `.xlsx/.xlsm` из Telegram, собирает backend import payload и отправляет его в `POST /api/v1/imports`.
- Добавлен чеклист Windows-приёмки с backend flags.

Блокеры перед релизом:

1. DNS `api.taksklad.uz` не резолвится; нужна A-запись `api -> 135.181.245.84`.
2. Нужен реальный Telegram upload test на копии рабочего Excel-файла.
3. Нужна ручная Windows-приёмка с backend flags.
4. Windows archive и `version.json` не менять до приёмки.

Следующий шаг:

1. Настроить DNS.
2. Прогнать реальную интеграционную проверку SkladBot/Telegram на копии живых заказов.
3. Перейти к Windows acceptance для desktop backend bridge.
